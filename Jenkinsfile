pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build started!'
            }
        }

        stage('Run Script') {
            steps {
                sh 'echo "Running script from repo"'
            }
        }
    }
}

